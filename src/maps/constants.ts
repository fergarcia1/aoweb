/** Lado del mapa en tiles (todos los mapas son cuadrados). */
export const MAP_TILE_SIZE = 72;

/** Escala desde la grilla base de diseño (18×18 → 72×72). */
export const MAP_SCALE = 4;

export const MAP_BASE_TILES = MAP_TILE_SIZE / MAP_SCALE;
